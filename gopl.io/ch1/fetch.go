package main

/* func main1() {
	count := make(map[string]int)
	input := bufio.NewScanner(os.Stdin)
	for input.Scan() {
		if input.Text() == "end" {
			break
		}
		count[input.Text()]++
	}

	for k, v := range count {
		fmt.Printf("%s %d\n", k, v)
	}
}
*/
